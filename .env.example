# Base URL of the backend REST API (must include /api)
VITE_API_URL=http://localhost:5000/api

# Mapbox GL access token, used for the click-to-pick location field
# in Trip and Branch forms (get one at https://account.mapbox.com/)
VITE_MAPBOX_TOKEN=pk.your-mapbox-token-here
